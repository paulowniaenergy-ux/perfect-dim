import { ArrowRight } from 'lucide-react';
import { SiteFooter } from '@/components/site-footer';
import { SiteHeader } from '@/components/site-header';
import { getPublishedArticles } from '@/lib/articles-repository';
export const dynamic = 'force-dynamic';
export default async function ArticlesPage() { const result = await getPublishedArticles(); const articles = result.ok ? result.data : []; return <main className="min-h-screen bg-[#f6f1e8]"><SiteHeader /><section className="pd-container py-16 lg:py-24"><p className="pd-eyebrow">Журнал Perfect Dim</p><h1 className="mt-4 max-w-4xl text-5xl leading-[.98] text-[#173326] sm:text-7xl">Статті про будинок, які допомагають приймати рішення</h1><p className="mt-7 max-w-2xl leading-7 text-[#647368]">Розбираємо конструктив, комунікації, ділянку та етапи будівництва — без загальних порад і прихованих нюансів.</p>{articles.length ? <div className="mt-14 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{articles.map((article) => <a key={article.id} href={`/articles/${article.slug}`} className="group overflow-hidden bg-white transition hover:-translate-y-1 hover:shadow-xl"><div className="aspect-[16/10] bg-[#e8dec9]">{article.coverImageUrl && <img src={article.coverImageUrl} alt="" className="h-full w-full object-cover" />}</div><div className="p-6"><p className="pd-eyebrow">{article.publishedAt ? new Intl.DateTimeFormat('uk-UA', { day: 'numeric', month: 'long', year: 'numeric' }).format(new Date(article.publishedAt)) : 'Стаття'}</p><h2 className="mt-4 text-2xl leading-tight text-[#173326]">{article.title}</h2><p className="mt-4 line-clamp-3 text-sm leading-6 text-[#647368]">{article.excerpt}</p><span className="mt-6 inline-flex items-center gap-2 text-sm font-semibold text-[#173326]">Читати <ArrowRight className="size-4" /></span></div></a>)}</div> : <div className="mt-14 border border-[#173326]/10 bg-white/45 p-10 text-[#647368]">Корисні матеріали готуються до публікації.</div>}</section><SiteFooter /></main>; }
